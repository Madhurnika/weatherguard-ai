import { Cloud, Droplets, Gauge, Thermometer, Umbrella, Wind } from 'lucide-react'; import type { CurrentWeather } from '../types/weather';
export default function WeatherStats({ current }: { current: CurrentWeather }) { const stats = [[Thermometer, 'Temperature', `${current.temp}°C`, 'Feels like'], [Droplets, 'Humidity', `${current.humidity}%`, 'Moisture level'], [Wind, 'Wind speed', `${current.windSpeed} m/s`, 'Current gust'], [Gauge, 'Pressure', `${current.pressure} hPa`, 'Sea level'], [Cloud, 'Cloud cover', `${current.cloudiness}%`, 'Sky coverage'], [Umbrella, 'Rain chance', `${current.rainProbability}%`, 'Next hours']]; return <section><div className="section-heading"><div><p className="eyebrow">AT A GLANCE</p><h2>Weather metrics</h2></div></div><div className="stats-grid">{stats.map(([Icon, label, value, note]) => <article className="stat-card" key={String(label)}><Icon size={19} /><small>{label as string}</small><strong>{value as string}</strong><span>{note as string}</span></article>)}</div></section>; }
