import type { Risk } from '../utils/weatherRisk';
export default function RiskAlert({ risks }: { risks: Risk[] }) { const serious = risks.filter(risk => risk.level !== 'SAFE'); return <section id="alerts" className="alert-panel"><div className="section-heading"><div><p className="eyebrow">EARLY WARNING SYSTEM</p><h2>Risk monitor</h2></div><span className={`risk-pill ${serious.length ? 'warning' : 'safe'}`}>{serious.length ? `${serious.length} active` : 'All clear'}</span></div>{risks.map(risk => <article className={`risk-item ${risk.level.toLowerCase()}`} key={risk.type}><span className="risk-dot" /><div><strong>{risk.type}</strong><p>{risk.reason}</p><small>Action: {risk.action}</small></div><b>{risk.level}</b></article>)}<small className="disclaimer">WeatherGuard AI system-generated risk assessment, not an official meteorological warning.</small></section>; }
