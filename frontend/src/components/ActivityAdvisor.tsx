import type { CurrentWeather } from '../types/weather'; export default function ActivityAdvisor({ current }: { current: CurrentWeather }) { const indoor = current.rainProbability > 50 || current.windSpeed > 10; return <section className="info-panel"><p className="eyebrow">PLAN YOUR DAY</p><h2>What can I do today?</h2><div className="activity-columns"><div><small>GOOD FOR</small><p>{indoor ? 'Reading · Cafés · Photography' : 'Walking · Cycling · Running · Photography'}</p></div><div><small>BETTER INDOORS</small><p>{indoor ? 'Outdoor sports · Long drives' : 'Long meetings · Heavy exercise'}</p></div></div></section>; }
