interface Props { favorites: string[]; recent: string[]; onSelect: (city: string) => void; onRemove: (city: string) => void; } export default function FavoriteLocations({ favorites, recent, onSelect, onRemove }: Props) { return <aside className="locations-panel"><p className="eyebrow">YOUR PLACES</p><h2>Saved locations</h2>{favorites.length ? favorites.map(city => <button className="place-row" key={city} onClick={() => onSelect(city)}><span>★</span>{city}<i onClick={event => { event.stopPropagation(); onRemove(city); }}>×</i></button>) : <p className="muted">Save places with the star button.</p>}<h3>Recent locations</h3>{recent.map(city => <button className="recent-row" key={city} onClick={() => onSelect(city)}>{city}<span>↗</span></button>)}</aside>; }
