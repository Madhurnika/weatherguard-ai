import { MapContainer, Marker, Popup, TileLayer } from 'react-leaflet'; import L from 'leaflet'; import type { WeatherPayload } from '../types/weather';
const marker = L.icon({ iconUrl: 'https://unpkg.com/leaflet@1.9.4/dist/images/marker-icon.png', iconRetinaUrl: 'https://unpkg.com/leaflet@1.9.4/dist/images/marker-icon-2x.png', shadowUrl: 'https://unpkg.com/leaflet@1.9.4/dist/images/marker-shadow.png', iconSize: [25, 41], iconAnchor: [12, 41] });
export default function WeatherMap({ weather }: { weather: WeatherPayload }) { return <section id="map" className="map-panel"><div className="section-heading"><div><p className="eyebrow">LOCATION INTELLIGENCE</p><h2>Weather map</h2></div><span className="section-note">{weather.location.lat.toFixed(3)}°, {weather.location.lon.toFixed(3)}°</span></div><MapContainer center={[weather.location.lat, weather.location.lon]} zoom={9} scrollWheelZoom className="map"><TileLayer attribution='&copy; OpenStreetMap contributors' url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png" /><Marker position={[weather.location.lat, weather.location.lon]} icon={marker}><Popup><strong>{weather.location.name}</strong><br />{weather.current.temp}°C · {weather.current.condition}</Popup></Marker></MapContainer></section>; }
