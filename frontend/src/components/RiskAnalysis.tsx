import type { Risk } from '../utils/weatherRisk'; export default function RiskAnalysis({ risks }: { risks: Risk[] }) { return <section className="info-panel"><p className="eyebrow">WHY IT MATTERS</p><h2>Risk analysis</h2><div className="risk-summary">{risks.map(risk => <div key={risk.type}><b>{risk.level === 'SAFE' ? '✓' : '!'} </b><span>{risk.type}<small>{risk.action}</small></span></div>)}</div></section>; }
