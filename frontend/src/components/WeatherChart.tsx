import { useState } from 'react'; import { Area, AreaChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'; import type { ForecastPoint } from '../types/weather';
export default function WeatherChart({ items }: { items: ForecastPoint[] }) { const [mode, setMode] = useState<'temp' | 'rain' | 'humidity' | 'wind'>('temp'); const labels = { temp: 'Temperature', rain: 'Rain probability', humidity: 'Humidity', wind: 'Wind speed' }; const data = items.map(item => ({ ...item, label: new Date(item.time).toLocaleTimeString([], { hour: 'numeric' }), temp: item.temp, rain: item.rainProbability, humidity: item.humidity, wind: item.windSpeed })); return <section className="chart-panel"><div className="section-heading"><div><p className="eyebrow">WEATHER INTELLIGENCE</p><h2>{labels[mode]} trend</h2></div><select value={mode} onChange={event => setMode(event.target.value as typeof mode)} aria-label="Choose weather trend"><option value="temp">Temperature</option><option value="rain">Rain chance</option><option value="humidity">Humidity</option><option value="wind">Wind speed</option></select></div><div className="chart"><ResponsiveContainer width="100%" height="100%"><AreaChart data={data}><defs><linearGradient id="chartFill" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#35c4bd" stopOpacity={0.45} /><stop offset="100%" stopColor="#35c4bd" stopOpacity={0.02} /></linearGradient></defs><CartesianGrid strokeDasharray="3 3" stroke="#ffffff18" /><XAxis dataKey="label" tick={{ fill: '#92a5b9', fontSize: 11 }} /><YAxis tick={{ fill: '#92a5b9', fontSize: 11 }} /><Tooltip contentStyle={{ background: '#122033', border: '1px solid #ffffff20', borderRadius: 8 }} /><Area type="monotone" dataKey={mode} stroke="#47d2c8" strokeWidth={3} fill="url(#chartFill)" /></AreaChart></ResponsiveContainer></div></section>; }
