import { LocateFixed, Search } from 'lucide-react';
interface Props { value: string; setValue: (value: string) => void; onSearch: () => void; onLocate: () => void; locating: boolean; }
export default function SearchBar({ value, setValue, onSearch, onLocate, locating }: Props) { return <section className="search-row"><div className="search-box"><Search size={19} /><input aria-label="Search city" value={value} onChange={event => setValue(event.target.value)} onKeyDown={event => event.key === 'Enter' && onSearch()} placeholder="Search city..." /><button onClick={onSearch}>Search</button></div><button className="locate-button" onClick={onLocate} disabled={locating}><LocateFixed size={18} />{locating ? 'Detecting location...' : 'Use my current location'}</button></section>; }
