import type { DailyForecast } from '../types/weather';
export default function WeeklyForecast({ items }: { items: DailyForecast[] }) { return <section><div className="section-heading"><div><p className="eyebrow">SEVEN DAY VIEW</p><h2>Weekly forecast</h2></div></div><div className="weekly-list">{items.map(item => <article key={item.date}><b>{item.day}</b><img src={`https://openweathermap.org/img/wn/${item.icon}.png`} alt={item.condition} /><span>{item.condition}</span><strong>{item.high}° <i>{item.low}°</i></strong><small>{item.rainProbability}% rain</small></article>)}</div></section>; }
