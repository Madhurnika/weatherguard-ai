import type { CurrentWeather } from '../types/weather'; import { weatherScore } from '../utils/weatherScore'; export default function WeatherScore({ current }: { current: CurrentWeather }) { const score = weatherScore(current); return <section className="score-panel"><div className="score-ring" style={{ '--score': `${score.value * 3.6}deg` } as React.CSSProperties}><div><strong>{score.value}</strong><small>/ 100</small></div></div><div><p className="eyebrow">COMFORT INDEX</p><h2>Weather comfort score</h2><p>{score.explanation}</p><small className="disclaimer">WeatherGuard AI derived score, not an official meteorological index.</small></div></section>; }
