import type { CurrentWeather } from '../types/weather'; import { recommendations } from '../utils/recommendations'; export default function SmartRecommendations({ current }: { current: CurrentWeather }) { return <section className="info-panel"><p className="eyebrow">PERSONALIZED GUIDANCE</p><h2>Smart recommendations</h2><div className="recommendations">{recommendations(current).map(item => <span key={item}>{item}</span>)}</div></section>; }
