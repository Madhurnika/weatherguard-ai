import { CloudSun, Moon, RefreshCw, Sun, Star } from 'lucide-react';
interface Props { unit: 'C' | 'F'; setUnit: (unit: 'C' | 'F') => void; dark: boolean; setDark: (value: boolean) => void; onRefresh: () => void; favorite: boolean; onFavorite: () => void; demo: boolean; }
export default function Navbar({ unit, setUnit, dark, setDark, onRefresh, favorite, onFavorite, demo }: Props) { return <header className="topbar"><div className="brand"><div className="brand-mark"><CloudSun size={24} /></div><div><strong>WeatherGuard <em>AI</em></strong><small>Smart weather detection & early warning</small></div></div><nav><a href="#dashboard">Dashboard</a><a href="#alerts">Alerts</a><a href="#map">Weather map</a><a href="#about">About</a></nav><div className="toolbar"><span className="status">{demo ? 'DEMO MODE' : 'LIVE DATA'}</span><button className="icon-button" title="Toggle temperature unit" onClick={() => setUnit(unit === 'C' ? 'F' : 'C')}>{unit === 'C' ? '°C' : '°F'}</button><button className="icon-button" title="Toggle theme" onClick={() => setDark(!dark)}>{dark ? <Sun size={17} /> : <Moon size={17} />}</button><button className="icon-button" title="Refresh weather" onClick={onRefresh}><RefreshCw size={17} /></button><button className={`icon-button ${favorite ? 'active' : ''}`} title="Save favorite" onClick={onFavorite}><Star size={17} fill={favorite ? 'currentColor' : 'none'} /></button></div></header>; }
