import type { ForecastPoint } from '../types/weather';
export default function HourlyForecast({ items }: { items: ForecastPoint[] }) { return <section><div className="section-heading"><div><p className="eyebrow">NEXT 12 HOURS</p><h2>Hourly outlook</h2></div><span className="section-note">Local time</span></div><div className="forecast-strip">{items.map(item => <article className="hour-card" key={item.time}><b>{new Date(item.time).toLocaleTimeString([], { hour: 'numeric' })}</b><img src={`https://openweathermap.org/img/wn/${item.icon}.png`} alt="" /><strong>{item.temp}°</strong><span>{item.rainProbability}% rain</span></article>)}</div></section>; }
