import type { CurrentWeather } from '../types/weather'; export default function TravelAdvisor({ current }: { current: CurrentWeather }) { const avoid = current.rainProbability > 75 || current.visibility < 3; const caution = current.rainProbability > 35 || current.windSpeed > 9; const state = avoid ? 'Avoid unnecessary travel' : caution ? 'Use caution' : 'Safe to travel'; return <section className={`travel-panel ${avoid ? 'danger' : caution ? 'caution' : 'good'}`}><p className="eyebrow">TRAVEL SAFETY ADVISOR</p><h2>{state}</h2><p>{avoid ? 'Conditions may make roads hazardous. Delay non-essential journeys.' : caution ? 'Leave extra time and stay alert for changing conditions.' : 'No major travel disruptions are indicated by this prototype assessment.'}</p><small className="disclaimer">Prototype-derived advisory, not an official travel or emergency service.</small></section>; }
