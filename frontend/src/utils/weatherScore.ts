import type { CurrentWeather } from '../types/weather';
export function weatherScore(current: CurrentWeather) { let score = 100; score -= Math.min(25, Math.abs(current.temp - 23) * 2); score -= Math.min(15, Math.abs(current.humidity - 50) * 0.3); score -= Math.min(15, current.windSpeed * 1.5); score -= Math.min(20, current.rainProbability * 0.2); score -= current.visibility < 5 ? 15 : 0; score -= Math.max(0, current.uvIndex - 6) * 2; const value = Math.max(0, Math.min(100, Math.round(score))); return { value, explanation: value >= 80 ? 'Comfortable outdoor conditions with moderate humidity and low rain probability.' : value >= 55 ? 'Mixed conditions. Plan breaks and check the latest risks before heading out.' : 'Challenging conditions. Consider indoor plans and take appropriate precautions.' }; }
